import { GraphKeyHandlerDirective } from './graph-key-handler.directive';

describe('GraphKeyHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new GraphKeyHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
